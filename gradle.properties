org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# JDK 17 fixo: o AGP 8.10 + Room 2.8.x exigem JVM 17+. Sem isso o build falha
# com "Dependency requires at least JVM runtime version 17" no room-gradle-plugin.
# Travado aqui pra não depender do JAVA_HOME do shell (cada PowerShell novo
# nasce sem ele).
org.gradle.java.home=C\:\\opt\\jdk-17.0.13+11

